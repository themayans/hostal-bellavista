# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Ruby
vendor/
.bundle/

# Node (build tooling only — nothing here ships)
node_modules/

# Raw, unoptimised photo drop-zone. Originals stay on your machine;
# only the optimised WebP under assets/images/ is committed.
_incoming/

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp
