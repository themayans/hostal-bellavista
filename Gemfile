# Pins the exact toolchain GitHub Pages runs, so a local preview matches the
# deployed site. CI does not use this file — the Pages Action supplies the same
# environment — but `docker compose up` and any local `bundle exec jekyll` do.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
